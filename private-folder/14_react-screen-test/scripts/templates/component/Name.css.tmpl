/* {{NAME}} — Figma node {{NODE_ID}} */
/* Figma variables used by this component:
{{FIGMA_VARS}}
   (bind via var(--cds-*) — check src/styles/tokens.css for exact names)
*/

.chord-{{NAME_KEBAB}} {
  /* TODO: implement using var(--cds-*) tokens above */
}
