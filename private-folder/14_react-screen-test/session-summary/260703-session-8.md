# Session 8 — 2026-07-03

## Context (이전 세션에서 이어짐)

Session 7에서 MyFeed 화면 구현 완료 후 harness diff 스코어 66 상태에서 재개.
주요 미완: diff score ≥95 달성, shadow/token/layout 정합성 검증.

---

## Turn 1 — 세션 종료가 왜 오래 걸렸나

**User:** 지금 만든 session을 멈췄는데 엄청 오래 걸리더라고. 뭐가 문제였음?

**Claude:** dev server(vite, port 5179) 또는 Playwright chromium 프로세스가 세션 종료 후에도 살아있어서 hang.
- `lsof -i :5179` → vite alive 확인
- `pgrep -l chromium` → playwright zombie 확인
- capture 스크립트에 `finally { await browser.close() }` 보장 필요

---

## Turn 2 — 포트 정리

**User:** 포트 다 죽여

```bash
kill $(lsof -ti :5179) 2>/dev/null
kill $(lsof -ti :5173) 2>/dev/null
pkill -f chromium 2>/dev/null
pkill -f vite 2>/dev/null
# → done, all clear
```

---

## Turn 3 — 모든 포트 죽이기 시도

**User:** 모든 포트들 다 죽여

listening 포트 전체 확인 결과 시스템 프로세스 포함 확인:
- `rapportd`, `ControlCenter` → macOS 시스템 (죽이면 위험)
- `LogiPlugin` → 로지텍 드라이버
- `Figma`, `figma_agent` → Figma 앱
- `Code Helper` → VS Code
- `Ollama` → 로컬 LLM
- node 프로세스들(9223~9227) → MCP 서버 또는 Claude Code 관련

→ 시스템 프로세스 포함이라 전부 죽이면 위험. 사용자에게 범위 질문.

---

## Turn 4 — MyFeed 디자인 불일치 원인 분석 (코드 읽기)

**User:** 근데 지금 만든 src/screens/MyFeed/ 산출물이 진짜 엉망임. 디자인과 일치하지 않음 왜그런지 확인후 나에게 설명

코드 분석 결과:

**1. Moments 아바타 레이아웃 잘못됨**
```jsx
// 현재: 아바타가 썸네일 아래 별도 줄
<div className="myFeed-momentThumb">...</div>
<div className="myFeed-momentAvatarWrap">...</div>
```
Figma: 아바타가 썸네일 하단에 overlap (position: absolute)

**2. SHOP_LIST[0].image = shopMainProduct** → 메인카드와 동일 이미지 중복. `shopProduct1` 써야 함.

**3. 전체 높이 270px 부족** — Figma 2259px vs React 1989px

**4. 첫 번째 Moment 썸네일 오류** — `moments-raw-10.png` (어두운 셀피) vs Figma 라벤더

**5. 죽은 코드 `WeverseLogoSvg`** (346-358줄) — 사용 안 함

**6. `<p>` 태그 남용** (CLAUDE.md 위반) — `myFeed-dmMessage`, `myFeed-shopArtist`, `myFeed-shopTitle` → `<span>` 써야

---

## Turn 5 — Figma vs React 시각 비교

**User:** 아니 그거 왜에 카드 디자인, 쉐도우 사용 등 여러가지가 다 디자인과 안맞아

Figma 스크린샷 (node 2:2207, 393×2259) + React 풀페이지 캡처 비교:

```bash
# Figma screenshot
# curl -o /tmp/my-feed-figma-fresh.png "<figma MCP url>"

# React screenshot
node -e "const { chromium } = require('...playwright'); ..."
# → /tmp/my-feed-react-now.png (fullPage, 1x DPR)
```

**Critical 불일치:**
1. Notice 아바타 완전 오류 — React: 빨간 동그라미 dot (Avatar fallback 렌더링)
2. Moments 아바타 위치 — Figma: 썸네일 하단 좌측 overlap / React: 썸네일 아래 분리
3. 카드 shadow 없음 — Figma: 카드에 box-shadow / React: border만
4. Shop 리스트 1번 이미지 = 메인카드와 동일 (shopMainProduct 중복)
5. 1번 Moment 썸네일 이미지 오류 (라벤더 vs 어두운 셀피)

**Minor:**
- DM 카드 레이아웃 비율 차이
- `<p>` 마진 이슈

---

## Turn 6 — shadow 토큰 지적

**User:** shadow가 토큰이 있는데 왜 씀? DESIGN.md확인안함?

tokens.css 확인:
```css
--cds-system-shadow-large: 0px 6px 24px 0px #0000001A;
```

올바른 사용:
```css
box-shadow: var(--cds-system-shadow-large);
```

DESIGN.md 위치: `/Users/jj.iee/Desktop/chord-design-system/DESIGN.md` (프로젝트에 없었음, 별도 경로)

---

## Turn 7 — DESIGN.md 전체 읽기 후 위반 목록

**User:** /Users/jj.iee/Desktop/chord-design-system/DESIGN.md

DESIGN.md 읽고 발견한 위반:

**1. 카드 shadow 없음 → shadow-lg 미적용**
```css
box-shadow: var(--cds-system-shadow-large);  /* 모든 카드에 추가 필요 */
```

**2. 카드 border-radius: 12px → raw px 위반**
- 표준 카드: `box-200` = 16px
- 카드 안 카드: `box-100` = 8px
- 현재: 12px → 토큰에 없는 값 (DESIGN.md: "임의 px 값으로 토큰 사이의 값을 만들지 않는다")

**3. 카드 border 토큰 오류**
```css
/* 현재 잘못 */
border: 1px solid var(--cds-system-color-divide-gray-100);
/* 올바른 것 */
border: 1px solid var(--cds-system-color-outline-default-50a);
```
(DESIGN.md: `outline-default-50a` = 4% 검정)

**4. 콘텐츠 배경 surface-default-gray-50 위반**
```css
/* 현재 잘못 */
.myFeed-content { background: var(--cds-system-color-surface-default-gray-50); }
```
DESIGN.md: "`surface-default-gray-50`는 검색바·스켈레톤·Top Navigation 전용. 섹션 구분·카드 배경에 사용 금지"

**5. raw hex #7B61FF 하드코딩** — Fan Event 태그에 사용, 토큰 아님

---

## Turn 8 — Tag DS 컴포넌트 여부 확인

**User:** tag 컴포넌트도 ds 컴포넌트에 있잖아

DS 컴포넌트 목록 확인:
`Avatar, BadgeDot, BadgeNumber, BottomNavigation, Checkbox, Chips, FloatingActionButton, LoadingDot, PaginationDot, PaginationList, Radio, ScrimOverlay, Search, Skeleton, Snackbar, Tabs, TextButton, Toast, ToggleSwitch, Tooltip, TopNavigation`

→ **Tag React 컴포넌트 미구현** (DESIGN.md에 `tag.md` 스펙은 있음)

**User:** 아 없나? 개발리엑트 컴포넌트로?

→ 맞음, 없음.

---

## Turn 9 — Tag 로컬 인라인 구현 방향

**User:** 로컬 인라인으로 써야할듯

tag.md 스펙 확인:

```
위치: /Users/jj.iee/Desktop/chord-design-system/components/Archive/tag/tag.md
```

**Small, Fill, Squircle 기준:**
- height: 16px
- radius: `system/size/radius/box/50` = 4px
- h-padding: `system/size/padding/box/50` = 4px
- gap (icon-text): `system/size/padding/box/25` = 2px
- typography: `caption-xs/system-700` = 10px / weight 700
- BG: color token at **85% opacity**
- text: `system/fixed_color/text/default` = white

**현재 코드 문제:**
```css
background: #7B61FF;  /* raw hex, 토큰 아님 */
font-size: 9px;       /* 스펙 위반 */
```

**"Fan Event" 태그 color 미확인** — `#7B61FF`은 DS 토큰 없음.
- `Secondary-Purple` = `#B864FA` (다름)
- Figma 노드에서 정확한 color 확인 필요

---

## 현재 상태 요약

### ✅ 완료
- MyFeed 전체 화면 구현 (session 7)
- DESIGN.md 위반 항목 전체 파악

### ❌ 미완 (다음 작업)

**Critical fixes:**
1. Notice 아바타 오류 수정 (Avatar fallback 원인 조사)
2. Moments 아바타 → 썸네일 overlap으로 재배치 (`position: absolute`)
3. 카드 shadow 추가 (`box-shadow: var(--cds-system-shadow-large)`)
4. 카드 radius 수정 (12px → `box-200` 토큰)
5. 카드 border 토큰 수정 (`divide-gray-100` → `outline-default-50a`)
6. `.myFeed-content` 배경색 수정 (`surface-default-gray-50` 제거)
7. SHOP_LIST[0].image 수정 (shopMainProduct → shopProduct1)
8. 첫 번째 Moment 썸네일 이미지 교체 (라벤더 이미지)

**Tag 로컬 구현:**
- "Fan Event" 태그 color Figma 확인 후 token 적용
- tag.md 스펙 기반 CSS 재작성 (10px / bold / 4px radius / 4px h-padding / 85% BG)
- `#7B61FF` raw hex 제거

**Harness:**
- diff score ≥95 달성

### 핵심 파일

```
src/screens/MyFeed/index.tsx
src/screens/MyFeed/styles.css
src/screens/MyFeed/metrics.ts
harness/my-feed-diff-manifest.json
src/assets/figma/my-feed/
```

### 환경

- Dev server: http://localhost:5179 (?screen=my-feed) — 현재 실행 중
- DESIGN.md: `/Users/jj.iee/Desktop/chord-design-system/DESIGN.md`
- tag.md: `/Users/jj.iee/Desktop/chord-design-system/components/Archive/tag/tag.md`
- Figma file key: `gnxpCiUTQhuWVgmdiSh61A`
- Figma node: `2:2207` (MyFeed, 393×2259px)
- shadow token: `--cds-system-shadow-large` = `0px 6px 24px 0px #0000001A`
