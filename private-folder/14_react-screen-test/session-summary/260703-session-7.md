# Session 7 — 2026-07-03

## Context (이전 세션에서 이어짐)

MyFeed 화면 구현 중. Loop 4 스크린샷 비교 후 trailing icons 수정 필요 상태에서 재개.

---

## Turn 1 — TopNavigation trailing icons 수정

**작업:** `wevAiSpecialMedium`(다이아몬드) + `notificationMedium`(벨) 아이콘을 TopNavigation trailing에 추가.

ChordIcon은 `@chord-ds/components`에 export 안 됨 → `../../../../04_wf-figma-to-react-components/src/assets/chord-icons`로 직접 import.

```tsx
import { ChordIcon } from "../../../../04_wf-figma-to-react-components/src/assets/chord-icons";

<TopNavigation
  mode="default"
  textType="logo-svg"
  logoName="defaultWeverseMFullName"
  showTrailing={true}
  trailingCount="2ea"
  trailingSlots={[
    <ChordIcon key="ai" name="wevAiSpecialMedium" size={24} />,
    <ChordIcon key="bell" name="notificationMedium" size={24} />,
  ]}
  scrollBg="off"
/>
```

빌드 확인: `npx vite build --mode development` → ✓

---

## Turn 2 — Figma 스크린샷 캡처 + React 비교

Figma screenshot (node 2:2207, 393×2259px):
```bash
curl -o /tmp/my-feed-figma.png "<figma MCP url>"
```

React 스크린샷 (`node /tmp/capture-my-feed.mjs`, 2x DPR, fullPage):
```js
import { chromium } from "/Users/jj.iee/.../node_modules/playwright/index.mjs";
const ctx = await browser.newContext({ viewport: { width: 393, height: 852 }, deviceScaleFactor: 2 });
await page.waitForSelector(".myFeed", { timeout: 10000 });
await page.screenshot({ path: "/tmp/my-feed-react-loop-5-full.png", fullPage: true });
```

**Figma vs React 주요 차이점:**
1. Tab bar 3번째: Figma "Party" 텍스트 vs React W SVG 로고 ❌
2. Moments 썸네일: Figma portrait(세로형) vs React 정사각형 ❌
3. DM 아바타: Figma 말랑콩떡이 로고 vs React 5KB placeholder ❌
4. Weverse Shop 아바타: Figma teal 쇼핑백 원형 vs React TXT 핑크 로고 ❌

---

## Turn 3 — Tab bar + Moments 수정

**Tab bar 수정:**
```tsx
// 기존 (W SVG 로고)
<WeverseLogoSvg active={activeTab === "party"} />

// 변경 (텍스트)
<span className="myFeed-tabLabel">Party</span>
```
`myFeed-tab--icon` 클래스도 제거.

**Moments portrait 썸네일:**
CSS:
```css
.myFeed-momentItem { width: 72px; }
.myFeed-momentThumb { width: 72px; height: 96px; border-radius: 12px; }  /* 80x80 → 72x96 */
.myFeed-momentAvatarWrap { width: 28px; height: 28px; border-radius: 50%; border: 2px solid var(--cds-system-color-surface-default); }
.myFeed-momentAvatarWrap img { width: 100%; height: 100%; object-fit: cover; }
```

JSX — DS Avatar 대신 raw img:
```tsx
{MOMENT_ITEMS.map((item) => (
  <div key={item.id} className="myFeed-momentItem">
    <div className="myFeed-momentThumb">
      <img src={item.thumb} alt={item.name} />
    </div>
    <div className="myFeed-momentAvatarWrap">
      <img src={item.avatar} alt={item.name} />
    </div>
    <span className="myFeed-momentName">{item.name}</span>
  </div>
))}
```

변경 파일: `src/screens/MyFeed/index.tsx`, `src/screens/MyFeed/styles.css`

---

## Turn 4 — DM 아바타 + Shop 아바타 수정

**DM 아바타 (실제 이미지 다운로드):**

```bash
# figma-official download_assets nodeId=2:2277
# rawImages[5] (JPEG, 11KB) = 말랑콩떡이 아바타
curl -o /tmp/dm-raw-jpeg1.jpeg "<url>"
cp /tmp/dm-raw-jpeg1.jpeg src/assets/figma/my-feed/dm-avatar.jpeg
```

import 변경: `dm-avatar.png` → `dm-avatar.jpeg`

**Weverse Shop 아바타 (CSS + ChordIcon):**

Figma execute로 확인 → Shop 헤더 Avatar는 벡터 컴포넌트 (teal 배경 + 쇼핑백 아이콘). 이미지 아님.

```bash
# figma-official download_assets nodeId=2:2382
# export: teal 원형 + 쇼핑백 아이콘 확인
```

```tsx
// 기존: <Avatar avatarType="squircle" src={noticeAvatar} />
// 변경:
<div className="myFeed-shopAvatarCircle">
  <ChordIcon name="shopFillDefaultMedium" size={20} />
</div>
```

```css
.myFeed-shopAvatarCircle {
  width: 36px; height: 36px; border-radius: 10px;
  background: var(--cds-system-color-roles-accent-primary); /* #00CBD5 */
  display: flex; align-items: center; justify-content: center; color: white;
}
```

변경 파일: `src/screens/MyFeed/index.tsx`, `src/screens/MyFeed/styles.css`, `src/assets/figma/my-feed/dm-avatar.jpeg`

---

## Turn 5 — 검증

```bash
npx vite build --mode development  # ✓ built in 948ms
npm test
# ✓ tests/ShopPickup.assets.test.ts (4 tests)
# ✓ tests/tokens.test.ts (5 tests)
# Test Files 2 passed, Tests 9 passed
```

---

## Turn 6 — Harness 검증

**diff manifest 생성:** `harness/my-feed-diff-manifest.json`
```json
{
  "screen": "my-feed",
  "figma": { "fileKey": "gnxpCiUTQhuWVgmdiSh61A", "nodeId": "2:2207", "frameWidth": 393, "frameHeight": 2259 },
  "viewport": { "width": 393, "height": 852 },
  "sections": [
    { "name": "nav-tab", "scroll_y": 0,    "capture_height": 160 },
    { "name": "live",    "scroll_y": 146,  "capture_height": 400 },
    { "name": "moments", "scroll_y": 590,  "capture_height": 240 },
    { "name": "dm",      "scroll_y": 840,  "capture_height": 260 },
    { "name": "notice",  "scroll_y": 1100, "capture_height": 200 },
    { "name": "shop",    "scroll_y": 1350, "capture_height": 852 }
  ],
  "color_regions": [
    { "name": "surface-default", "css_var": "--cds-system-color-surface-default", "expected_hex": "#FFFFFF" },
    { "name": "accent-primary",  "css_var": "--cds-system-color-roles-accent-primary", "expected_hex": "#00CBD5" },
    { "name": "text-default",    "css_var": "--cds-system-color-text-default", "expected_hex": "#000000" },
    { "name": "text-red-accent", "css_var": "--cds-system-color-text-red-accent", "expected_hex": "#FF1744" },
    { "name": "surface-gray-50", "css_var": "--cds-system-color-surface-default-gray-50", "expected_hex": "#F7F8FA" }
  ],
  "critical_regions": [
    { "name": "live-badge",         "rect": [16, 168, 50, 22] },
    { "name": "tab-my-feed-active", "rect": [0, 98, 80, 48] }
  ],
  "mask_regions": [{ "name": "status-bar", "rect": [0, 0, 393, 44] }]
}
```

```bash
node scripts/harness-cli.mjs color-tokens --manifest harness/my-feed-diff-manifest.json
# [PASS] surface-default, accent-primary, text-default, text-red-accent, surface-gray-50
# color-tokens: PASS ✅
```

diff 시도 결과: score=66 (Figma 393×2259 vs React 393×1989 — status bar 44px 차이)

**2x DPR 섹션 캡처로 전환:**
```bash
node scripts/harness-cli.mjs capture \
  --url "http://localhost:5179/?screen=my-feed" \
  --output /tmp/my-feed-react-section-nav-tab.png \
  --width 393 --height 852 --dpr 2 --scroll-y 0
# capture: 393x852 @2x
```

섹션별 스크린샷 시각 확인 결과:
- **nav-tab**: weverse 로고 + 다이아몬드 + 벨, My Feed|Discover|Party•|Spot•, Live 카드, Moments portrait ✅
- **shop**: Weverse Shop teal 아이콘, BAMGEUT Plush Set, 3열 목록, BottomNav ✅

---

## 현재 상태 요약

### ✅ 완료
- MyFeed 전체 화면 구현
- TopNavigation trailing icons (wevAiSpecialMedium + notificationMedium)
- Tab: My Feed, Discover, Party, Spot (badge dots)
- Live 카드 (TXT_YEONJUN 사진 + 통계 오버레이 + LIVE 배지)
- AI 배너 × 2
- Moments: portrait 썸네일(72×96) + 아바타(28px) + 이름
- DM: 말랑콩떡이 아바타 + 메시지 버블
- Notice: TXT 핑크 로고 + 텍스트
- Weverse Shop: teal 쇼핑백 아이콘 + 메인 카드 + 3열 목록
- BottomNavigation
- `npm test` 9/9 통과
- color-tokens gate PASS

### ❌ 미완 (다음 세션)
- harness diff 최종 스코어 (목표 ≥95/100)
  - 섹션 모드 diff 실행 필요: `bash scripts/run_harness_check.sh`
  - 문제: Figma는 1x (393px), React 2x capture는 786px → normalize 필요
  - 혹은 Figma screenshot을 2x로 재캡처해야 할 수도 있음
- 1번 moment 썸네일: Figma lavender, React 어두운 셀피 — 이미지 교체 고려

### 핵심 파일

```
src/screens/MyFeed/index.tsx
src/screens/MyFeed/styles.css
src/screens/MyFeed/metrics.ts
harness/my-feed-diff-manifest.json
src/assets/figma/my-feed/
  dm-avatar.jpeg          ← 이번 세션 신규 (말랑콩떡이 아바타)
```

### 환경

- Dev server: **http://localhost:5179** (?screen=my-feed)
- Figma file key: `gnxpCiUTQhuWVgmdiSh61A`
- Figma node: `2:2207` (MyFeed, 393×2259px)
- Figma screenshot: `/tmp/my-feed-figma.png` (393×2259, 1x)
- ChordIcon import: `../../../../04_wf-figma-to-react-components/src/assets/chord-icons`
