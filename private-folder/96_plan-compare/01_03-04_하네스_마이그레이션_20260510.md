# 플랜: 03/04 폴더 → Claude 전용 하네스 패턴 마이그레이션

## Context

- 03_wf, 04_wf 둘 다 현재 Codex/Claude 양쪽 호환 구조 (`AGENTS.md` + `CLAUDE.md` → `docs/AI_RULES.md`)
- 사용자가 Claude 전용 하네스 패턴(이미지 카드 표준)으로 전환 결정
- 표준 구조: `CLAUDE.md` + `.claude/agents/` + `.claude/skills/` + `harness/` + `_workspace/`
- 기존 폴더가 풍부한 내용(workflow, rules, knowledge, AI_RULES.md, PLAYBOOK.md)을 가지고 있으므로 **삭제 X, 분류 매핑 O**
- 마이그레이션 사고를 대비해 `99_archive/`에 먼저 백업

## 매핑 원칙

| 성격 | 새 위치 |
|------|---------|
| 핵심 규칙 (Mission, Allowed/Forbidden, Source Priority, Done Criteria) | `CLAUDE.md` (직접) |
| 작업 흐름 / Read Order / Gate Matrix | `harness/orchestrator.md` |
| 단계별 작업 시퀀스 | `harness/workflow.md` |
| 반복 작업 매뉴얼 (validation, schema, contract 등) | `.claude/skills/*.md` |
| AI 역할 정의 | `.claude/agents/*.md` |
| 작업 중간 산출물 (drafts, artifacts) | `_workspace/` |
| 실제 코드/리소스 (src, dist, scripts, tools, refs) | 그대로 유지 |
| 기록물 (history, retrospectives, knowledge) | 그대로 유지 |

---

## Phase 1: 백업 (안전망)

```bash
# 03_wf 백업 (작음, 그대로 복사)
cp -R /Users/jj.iee/Desktop/workspace/03_wf-figma-to-description \
      /Users/jj.iee/Desktop/workspace/99_archive/03_wf-figma-to-description_legacy

# 04_wf 백업 (node_modules/dist/storybook-static 제외)
rsync -a --exclude='node_modules' --exclude='dist' --exclude='storybook-static' \
       --exclude='.serena' --exclude='.omc' \
       /Users/jj.iee/Desktop/workspace/04_wf-figma-to-react-components/ \
       /Users/jj.iee/Desktop/workspace/99_archive/04_wf-figma-to-react-components_legacy/
```

검증: `ls 99_archive/` 에 두 _legacy 폴더 존재 확인.

---

## Phase 2: 03_wf 마이그레이션

### 2.1 새 폴더 생성
```
.claude/agents/
.claude/skills/
harness/
_workspace/drafts/
_workspace/outputs/
```

### 2.2 파일 이동/변환 매핑

| 기존 경로 | 액션 | 새 경로 |
|-----------|------|---------|
| `AGENTS.md` | **삭제** | — (Claude 전용) |
| `docs/AI_RULES.md` | **분해** | `CLAUDE.md` + `harness/orchestrator.md` + skills/* |
| `CLAUDE.md` | **재작성** | AI_RULES.md 핵심 규칙 흡수, @ import 제거 |
| `PLAYBOOK.md` | **이동/통합** | `harness/orchestrator.md`로 흡수 |
| `workflow/README.md` | 이동 | `harness/workflow.md` |
| `workflow/figma-write-readback.md` | 이동 | `.claude/skills/figma-write-readback.md` |
| `workflow/validation-checklist.md` | 이동 | `.claude/skills/validate-description.md` |
| `workflow/description-yaml-schema.md` | 이동 | `.claude/skills/yaml-schema.md` |
| `workflow/exceptions.md` | 이동 | `.claude/skills/exceptions.md` |
| `workflow/` (빈 폴더) | 삭제 | — |
| `draft-descriptions/` | 이동 | `_workspace/drafts/` |
| `examples/` | 이동 | `_workspace/examples/` |
| `tools/`, `refs/`, `history/`, `retrospectives/`, `docs/`(빈) | **그대로 유지** | — |
| `README.md` | 그대로 유지 (참조 경로만 새 위치로 갱신) | — |

### 2.3 새 CLAUDE.md 구조
```markdown
# Figma Description Workflow

## Mission
Figma Component Description YAML 업데이트/검증 전용.
Figma Description이 SoT. 파생 component Markdown 만들지 않음.

## Read Order
1. 이 파일 (CLAUDE.md)
2. harness/orchestrator.md
3. harness/workflow.md
4. 단계별 .claude/skills/*.md
5. refs/figma-component-keys/index.md (필요 시)
6. history/figma-description-history.md (변경 시)

## Allowed / Forbidden / Source Priority / Done Criteria
... (AI_RULES.md에서 발췌)

## Skills 인덱스
- validate-description: YAML 검증 절차
- yaml-schema: 스키마 정의
- figma-write-readback: 쓰기 후 readback
- exceptions: 예외 처리
```

### 2.4 .claude/agents/ 초안
```
.claude/agents/
├── description-writer.md   (YAML 작성/업데이트 담당)
└── description-validator.md (검증/readback 담당)
```

---

## Phase 3: 04_wf 마이그레이션

### 3.1 새 폴더 생성
```
.claude/agents/
.claude/skills/rules/      (rules/ 6개 파일이 들어감)
harness/
_workspace/drafts/
_workspace/outputs/
```

(`.claude/`는 이미 존재 - plans/, settings.local.json 보존)

### 3.2 파일 이동/변환 매핑

| 기존 경로 | 액션 | 새 경로 |
|-----------|------|---------|
| `AGENTS.md` | **삭제** | — |
| `docs/AI_RULES.md` | **분해** | `CLAUDE.md` + `harness/orchestrator.md` + skills/* |
| `CLAUDE.md` | **재작성** | AI_RULES.md 핵심 흡수, @ import 제거 |
| `PLAYBOOK.md` | 통합 | `harness/orchestrator.md`로 흡수 |
| `workflow/component-build-loop.md` | 이동 | `harness/workflow.md` |
| `workflow/figma-source-read.md` | 이동 | `.claude/skills/figma-read.md` |
| `workflow/source-note-contract.md` | 이동 | `.claude/skills/source-note.md` |
| `workflow/validation-checklist.md` | 이동 | `.claude/skills/validate-build.md` |
| `workflow/icon-asset-registry.md` | 이동 | `.claude/skills/icon-registry.md` |
| `workflow/exceptions.md` | 이동 | `.claude/skills/exceptions.md` |
| `workflow/README.md` | 통합 | `harness/workflow.md`로 흡수 |
| `workflow/` (빈 폴더) | 삭제 | — |
| `rules/*.md` (6개) | 이동 | `.claude/skills/rules/*.md` |
| `rules/README.md` | 통합 | `.claude/skills/rules/README.md` |
| `knowledge/` | 이동 | `.claude/skills/knowledge/` |
| `artifacts/` | 이동 | `_workspace/artifacts/` |
| `src/`, `dist/`, `node_modules/`, `scripts/`, `tokens/`, `public/`, `storybook-static/`, `*.config.ts`, `package*.json`, `index.html`, `tsconfig.json` | **그대로 유지** | — |
| `history/`, `retrospectives/`, `docs/`(빈), `.storybook/` | **그대로 유지** | — |
| `figma.config.ts` | **그대로 유지** | — |
| `.gitignore`, `.serena/`, `.omc/`, `.claude/plans/`, `.claude/settings.local.json` | **그대로 유지** | — |

### 3.3 04 새 CLAUDE.md 핵심 추가
- `Read Order`에서 `rules/` → `.claude/skills/rules/` 경로로 갱신
- `knowledge/` → `.claude/skills/knowledge/` 경로로 갱신
- Gate Matrix 표는 `harness/orchestrator.md`로 분리, CLAUDE.md에는 요약만

### 3.4 .claude/agents/ 초안
```
.claude/agents/
├── source-note-writer.md       (Figma 읽고 source.md 작성)
├── component-builder.md        (React/Storybook 구현)
└── visual-validator.md         (visual:diff/baseline 검증)
```

### 3.5 코드 내부 경로 참조 갱신
- `src/figma/<component>.source.md` 안의 `workflow/` → `.claude/skills/` 참조
- `scripts/audit-*.mjs` 안의 path 상수 (있다면) 갱신
- `package.json` scripts 내부 path (있다면) 갱신

---

## Phase 4: 검증

### 4.1 구조 검증
```bash
# 두 폴더 모두 표준 5요소 존재 확인
for d in 03_wf-figma-to-description 04_wf-figma-to-react-components; do
  echo "=== $d ==="
  test -f "$d/CLAUDE.md" && echo "✓ CLAUDE.md"
  test -d "$d/.claude/agents" && echo "✓ .claude/agents"
  test -d "$d/.claude/skills" && echo "✓ .claude/skills"
  test -d "$d/harness" && echo "✓ harness"
  test -d "$d/_workspace" && echo "✓ _workspace"
  test ! -f "$d/AGENTS.md" && echo "✓ AGENTS.md 제거됨"
done
```

### 4.2 04_wf 빌드 검증 (코드가 살아있는지)
```bash
cd /Users/jj.iee/Desktop/workspace/04_wf-figma-to-react-components
npm test           # 기존 테스트 통과
npm run typecheck  # 타입 통과
```

### 4.3 백업 무결성
```bash
ls 99_archive/03_wf-figma-to-description_legacy/CLAUDE.md
ls 99_archive/04_wf-figma-to-react-components_legacy/src | head
```

### 4.4 Claude 동작 확인
- 새 CLAUDE.md를 Claude Code가 자동 로드하는지 (세션 재시작 후)
- `.claude/skills/` 항목이 Skill 시스템에 인식되는지 (필요 시 frontmatter 확인)

---

## 위험 / 주의사항

| 위험 | 완화책 |
|------|--------|
| 04의 `src/figma/*.source.md` 안에 옛 `workflow/` path 하드코딩 | grep으로 사전 검색 → 일괄 치환 |
| `package.json` 또는 `scripts/*.mjs`가 `rules/`, `knowledge/` path 사용 | grep → 새 path로 갱신 |
| `.claude/skills/`의 .md 파일이 Skill 시스템에 등록되려면 frontmatter 필요 | 각 파일 상단에 `---\nname: ...\ndescription: ...\n---` 추가 |
| AI_RULES.md 분해 시 규칙 누락 | 분해 후 원본과 항목 단위 대조 (Allowed/Forbidden 개수 일치 확인) |
| Codex 호환 영구 손실 | 99_archive에 _legacy 보존됨, 필요 시 복원 가능 |

---

## 실행 순서 요약

1. Phase 1: 백업 (두 폴더 _legacy로 복사)
2. Phase 2: 03_wf 먼저 (작고 단순) — 매핑 검증용
3. 03 검증 OK 후 → Phase 3: 04_wf 진행
4. Phase 4: 전체 검증

**03을 먼저 끝내고 한 번 확인받은 다음 04로 넘어가는 단계 분리 권장.**
